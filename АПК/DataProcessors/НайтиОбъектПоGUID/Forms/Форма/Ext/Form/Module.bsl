
&НаСервере
Процедура ПолучитьОбъектПоGUIDНаСервере() 
	
	GUIDОбъекта = "cc281d4f-f769-4739-b467-a88c24a21d91";
	
	СсылкаНаОбъект = Неопределено;
	
	СписокОбъектовМетаданных = Новый Массив;
	СписокОбъектовМетаданных.Добавить(Справочники);
	СписокОбъектовМетаданных.Добавить(Документы);
	СписокОбъектовМетаданных.Добавить(ПланыВидовХарактеристик);
	СписокОбъектовМетаданных.Добавить(ПланыСчетов);
	СписокОбъектовМетаданных.Добавить(ПланыОбмена);
	СписокОбъектовМетаданных.Добавить(БизнесПроцессы);
	СписокОбъектовМетаданных.Добавить(Задачи);
	
	Для Каждого текСтрМетаданных Из СписокОбъектовМетаданных Цикл
		
		Для Каждого текСтрМенеджер Из текСтрМетаданных Цикл
			
			ВременнаяСсылка = текСтрМенеджер.ПолучитьСсылку(Новый УникальныйИдентификатор(GUIDОбъекта));
			
			// Проверяем что ссылка не пустая, только потом вызываем ПолучитьОбъект()
			
			Если НЕ ВременнаяСсылка.Пустая() Тогда
				
				Попытка
					
					Объект = ВременнаяСсылка.ПолучитьОбъект();
					
					Если Объект <> Неопределено Тогда
						
						СсылкаНаОбъект = ВременнаяСсылка;
						Сообщить("Найден объект: " + СсылкаНаОбъект);
						
					КонецЕсли;
					
				Исключение
					// Ссылка есть, но объект недоступен — пропускаем	
				КонецПопытки;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Если СсылкаНаОбъект = Неопределено Тогда
		Сообщить("Объект с GUID " + GUIDОбъекта + " не найден.");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьОбъектПоGUID(Команда)
	ПолучитьОбъектПоGUIDНаСервере();
КонецПроцедуры
